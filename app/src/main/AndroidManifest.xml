<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.banqueenligne">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BanqueEnLigne"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddBeneficiaire"></activity>
        <activity android:name=".DetailBeneficiaire" />
        <activity android:name=".MesBeneficiaires" /> <!-- <activity android:name=".Connexion"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".Connexion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MakeVirement" />
        <activity android:name=".DepuisCompte" />
        <activity android:name=".VersCompte" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>